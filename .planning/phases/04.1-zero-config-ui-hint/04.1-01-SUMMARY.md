---
phase: 04.1-zero-config-ui-hint
plan: 01
status: complete
started: 2026-06-01T14:25:00+08:00
completed: 2026-06-01T14:30:00+08:00
duration: 5min
---

## Summary

Added API key detection modal to the Generate page. When no API key is configured, clicking "Generate Summary" shows a blocking dialog with "API Key Required" title, explanation text, and two actions: "Go to Settings" (navigates to /settings) and "Close" (dismisses modal). Existing generation flow with API key configured is completely unchanged.

## Self-Check: PASSED

- [x] `useConfig()` hook imported and used for config detection
- [x] `useNavigate()` from react-router for Settings navigation
- [x] Dialog components imported from existing `../ui/dialog`
- [x] `showApiKeyDialog` state controls modal visibility
- [x] Click-time detection (not page-load) per D-05
- [x] Fully blocking — no "proceed anyway" option per D-02
- [x] No backend files modified per D-03
- [x] `pnpm build` passes (TypeScript + Vite bundle)
- [x] Pre-existing test failures only (database migration count, git adapter local state)

## Deviations

None. Implementation follows the plan exactly.

## Key Files

### key-files.created

- `src/ui/components/generate/generate-form.tsx` (modified — API key check + dialog)

### key-files.dependencies

- `src/ui/components/ui/dialog.tsx` — Reused existing Dialog component
- `src/ui/hooks/use-config.ts` — Reused existing useConfig hook
